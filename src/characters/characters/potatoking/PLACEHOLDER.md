# Potato King — Placeholder States

`correct.json` and `wrong.json` are currently duplicates of `smile.json`.

Only the `smile` variant exists in Figma (file `nRp0M7stnwuRvbzApLsK8w`, node 76:490).

**Do not ship these states to production without replacing them with distinct art.**

Each file also carries a `"_placeholder"` field as a machine-readable flag.

Tracked: https://trello.com/c/aFPvQBrx
